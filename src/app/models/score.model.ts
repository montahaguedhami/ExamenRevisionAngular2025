/**
 * MODÈLE DE DONNÉES - SCORE
 * 
 * Cette classe représente le score d'un match.
 * Elle contient les points marqués par chaque équipe.
 */

export class Score {
  // Nombre de points marqués par l'équipe A
  equipeA!: number;
  
  // Nombre de points marqués par l'équipe B
  equipeB!: number;
}
